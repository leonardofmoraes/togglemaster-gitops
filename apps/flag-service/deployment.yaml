apiVersion: apps/v1
kind: Deployment
metadata:
  name: flag-service
  namespace: togglemaster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flag-service
  template:
    metadata:
      labels:
        app: flag-service
    spec:
      containers:
        - name: flag-service
          image: 407098684727.dkr.ecr.us-east-1.amazonaws.com/togglemaster-dev-flag-service:latest
          ports:
            - containerPort: 8002
          env:
            - name: PORT
              value: "8002"
            - name: DB_USER
              value: "toggleadmin"
            - name: DB_HOST
              value: "togglemaster-dev-flag-db.cfihdvxwcxdd.us-east-1.rds.amazonaws.com"
            - name: DB_NAME
              value: "flags_db"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: togglemaster-secrets
                  key: DB_PASSWORD
            - name: DATABASE_URL
              value: "postgres://$(DB_USER):$(DB_PASSWORD)@$(DB_HOST):5432/$(DB_NAME)"
            - name: AUTH_SERVICE_URL
              value: "http://auth-service.togglemaster.svc.cluster.local:8001"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "otel-collector-opentelemetry-collector.monitoring.svc.cluster.local:4317"
            - name: SERVICE_VERSION
              value: "1.0.0"
---
apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: togglemaster
spec:
  selector:
    app: flag-service
  ports:
    - port: 8002
      targetPort: 8002