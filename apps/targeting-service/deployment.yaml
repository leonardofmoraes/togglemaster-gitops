apiVersion: apps/v1
kind: Deployment
metadata:
  name: targeting-service
  namespace: togglemaster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: targeting-service
  template:
    metadata:
      labels:
        app: targeting-service
    spec:
      containers:
        - name: targeting-service
          image: 407098684727.dkr.ecr.us-east-1.amazonaws.com/togglemaster-dev-targeting-service:latest
          ports:
            - containerPort: 8003
          env:
            - name: PORT
              value: "8003"
            - name: DB_USER
              value: "toggleadmin"
            - name: DB_HOST
              value: "togglemaster-dev-targeting-db.cfihdvxwcxdd.us-east-1.rds.amazonaws.com"
            - name: DB_NAME
              value: "targeting_db"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: togglemaster-secrets
                  key: DB_PASSWORD
            - name: DATABASE_URL
              value: "postgres://$(DB_USER):$(DB_PASSWORD)@$(DB_HOST):5432/$(DB_NAME)"
            - name: AUTH_SERVICE_URL
              value: "http://auth-service.togglemaster.svc.cluster.local:8001"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "otel-collector-opentelemetry-collector.monitoring.svc.cluster.local:4317"
            - name: SERVICE_VERSION
              value: "1.0.0"
---
apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: togglemaster
spec:
  selector:
    app: targeting-service
  ports:
    - port: 8003
      targetPort: 8003